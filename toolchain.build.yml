version: v1.0
name: Compile tc
agent:
  machine:
    type: e1-standard-8
    os_image: ubuntu1804
blocks:
  - name: "Build"
    task:
      jobs:
      - name: Compiling tc
        commands:
          - checkout
          - install-package bison ca-certificates ccache clang cmake curl file flex gcc g++ git make ninja-build python3 texinfo zlib1g-dev libssl-dev libelf-dev patchelf
          - bash ./build.toolchain.sh












 









  










 




